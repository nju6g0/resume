import React, { useState } from "react";

import classes from "./styles.module.scss";
import classNames from "classnames/bind";
const cx = classNames.bind(classes);

const SprintTask = () => {


  return (
    <div className={cx("sprintTask")}>
     SprintTask
    </div>
  );
};
export default SprintTask;
